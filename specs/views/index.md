* receives:
    * db:
      * type: object
      * properties:
         * _id:
            * type: object
            * properties:
               * imageurl: string
               * upvotes: number
               * downvotes: number
               
* elements: 
    * link or button to /add and /random
    * table/carousel to display all images
