* receives:
    * db:
      * type: object
      * properties:
         * _id:
            type: object
            properties:
            * imageurl
            * upvotes
            * downvotes
            * _id

* elements: 
    * link or button to /add and /random
    * table/carousel to display all images
